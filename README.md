# Math10_FinalProject


Implement the codes of logistics regression for classification problem by using Numpy (A library for Python).
Implement the codes for Principle Component Analysis.
Using Scikit Learn package for supervised and unsupervised machine learning methods: k-nearest neighbors algorithm (k-NN) and t-distributed stochastic neighbor embedding (t-SNE).
Using cuML and TensorFlow packages to accelerate the machine learning program by GPU.
